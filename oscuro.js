$(document).ready(function() {
    // Evento clic para cambiar al modo oscuro
    $('#oscuro').click(function() {
        // Toggle de la clase dark-mode en el body
        $('body').toggleClass('dark-mode');
    });
});